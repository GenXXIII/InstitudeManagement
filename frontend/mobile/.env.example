# Optional override when Expo cannot derive the computer's LAN address.
# Replace YOUR_COMPUTER_IP with the IPv4 address shown for Wi-Fi by `ipconfig`.
EXPO_PUBLIC_API_URL=http://YOUR_COMPUTER_IP:5080
