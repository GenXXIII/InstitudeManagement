import type { ClassroomItem } from "../types/classroom";
import { ManagementActions } from "./management-actions";

export function RoomBoard({ items, onEdit, onDeactivate }: { items: ClassroomItem[]; onEdit: (item: ClassroomItem) => void; onDeactivate: (item: ClassroomItem) => void }) {
  return <section className="panel horizontal-management-table room-horizontal"><div className="horizontal-management-head"><span>Learning space</span><span>Building</span><span>Department ownership</span><span>Capacity</span><span>Attendance device</span><span>Study status</span><span>Actions</span></div>{items.map(item => <article className="horizontal-management-row" key={item.id}><div className="room-code-horizontal"><strong>{item.values.code}</strong><span>{item.values.roomType}</span></div><div className="horizontal-detail"><span>Building</span><strong>{item.values.building}</strong></div><div className="horizontal-detail"><span>Assigned department</span><strong>{item.values.department}</strong></div><div className="horizontal-detail"><span>Maximum occupancy</span><strong>{item.values.capacity} seats</strong></div><div className="device-detail"><i className={item.values.deviceOnline === "true" ? "online" : ""}/><strong>{item.values.deviceOnline === "true" ? "Online" : "Offline"}</strong></div><span className={`table-status ${item.values.studyStatus.toLowerCase().replace(" ", "-")}`}>{item.values.studyStatus}</span><ManagementActions item={item} onEdit={onEdit} onDeactivate={onDeactivate}/></article>)}</section>;
}
